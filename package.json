{
  "name": "opencode-prompt-router",
  "version": "0.1.0",
  "description": "TF-IDF skill router plugin for OpenCode — auto-loads matching skills based on user prompts",
  "main": "dist/index.mjs",
  "types": "index.ts",
  "files": [
    "dist/",
    "index.ts",
    "core/"
  ],
  "scripts": {
    "build": "bun build index.ts --outfile dist/index.mjs --target bun --format esm",
    "prepublishOnly": "bun run build",
    "test": "bun test"
  },
  "keywords": [
    "opencode",
    "opencode-plugin",
    "skills",
    "routing"
  ],
  "author": "Anders Sveen <anders@f12.no>",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/anderssv/opencode-prompt-router.git"
  },
  "license": "MIT",
  "peerDependencies": {
    "@opencode-ai/plugin": "^1.4.10"
  }
}
